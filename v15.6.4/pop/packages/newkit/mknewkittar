#!/bin/csh
# create a tar file for $poplocal/local/newkit
# Aaron Sloman
# 4 Jul 1999

cd $poplocal/local

ls -l newkittar.gz

rm -f newkittar.gz

tar cf newkittar newkit

ls -l newkittar

gzip newkittar

ls -l newkittar.gz
